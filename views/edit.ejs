<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Updating Task</title>
    <link rel="stylesheet" href="createstyle.css">
</head>
<body>
    <div class="wrapper">
        <form action="/update?_id=<%= Tasks._id %>" method="post">
            <h1>Update your Task</h1>
            <div class="input-box">
                <input type="text" value=<%= Tasks.taskname %> name="taskName" required>
            </div>
            <div class="input-box">
                <input type="text" value=<%= Tasks.deadline %> name="deadlineDate" required>
            </div>
            <div class="input-box-detail">
                <input type="text" value=<%= Tasks.content %> name="taskContent" required>
            </div>
            <button type="submit" class="btn">update</button>
        </form>
    </div>
</body>
</html>